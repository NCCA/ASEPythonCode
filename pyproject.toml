[project]
name = "asepythoncode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.2",
]

[tool.uv.workspace]
members=["Lecture2","Lecture3"]
exclude =["*"]
